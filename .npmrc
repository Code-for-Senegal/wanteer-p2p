# Expo/Metro and Next.js resolve native and transitive dependencies from a flat
# node_modules layout; the isolated store breaks both in a workspace.
node-linker=hoisted
strict-peer-dependencies=false
auto-install-peers=true
