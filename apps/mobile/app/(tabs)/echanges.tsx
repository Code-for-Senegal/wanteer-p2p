import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function MyListingsScreen() {
  return (
    <PlaceholderScreen
      title="Mes annonces"
      description="Les annonces publiées depuis ce téléphone, avec « marquer comme donné » et la suppression."
    />
  );
}
